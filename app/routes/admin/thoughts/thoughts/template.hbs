<div class="container-fluid slice">
  <div class="container align-self-center">
    <div class="form-horizontal">
        <br>
        <div class="form-group">
            <label class="col-sm-2 control-label">Name:</label>
            <div class="col-sm-10">
                {{input type="text" value=Newname class="form-control" placeholder="The name of the Library"}}
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-2 control-label">Date:</label>
            <div class="col-sm-10">
                {{input type="date" value=Newdate class="form-control" placeholder="The name of the Library"}}
            </div>
        </div>
        {{!-- <div class="form-group">
            <label class="col-sm-2 control-label">Article</label>
            <div class="col-sm-10">
                {{textarea value=Newarticle placeholder="Input your article here..." class="form-control" row="7"}}
            </div>
        </div> --}}
        <div class="form-group">
            <label class="col-sm-6 control-label">Image-Person (Drag & Drop Here):</label>
            <div class="col-sm-6">
                {{!-- {{input type="file" id="fileInput" class="form-control" action="encodeImageFileAsURL"}} --}}
                {{tinymce-editor options=imageOptions value=newImgPerson}}
            </div>
        </div>
        <div class="form-group">
            {{tinymce-editor options=ownOptions value=Newthought}}
        </div>

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default" {{action 'saveThought'}}>Add to Thought List</button>
            </div>
        </div>
    </div>

    {{#if responseMessage}}
     <div class="alert alert-success">{{responseMessage}}</div>
   {{/if}}

  </div>
</div>

{{outlet}}